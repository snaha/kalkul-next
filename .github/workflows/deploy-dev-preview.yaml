name: Deploy dev PR previews

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

concurrency: preview-${{ github.ref }}

env:
  PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}
  PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
  VITE_ADAPTER: static
  STRIPE_API_KEY: ${{ secrets.STRIPE_API_KEY }}
  PUBLIC_ORIGIN: ${{ vars.PUBLIC_ORIGIN_DEV }}
  PUBLIC_DISABLE_PAYWALL: ${{ vars.PUBLIC_DISABLE_PAYWALL }}
  VITE_API_URL: ${{ vars.PREVIEW_VITE_API_URL }}
  PUBLIC_DISCORD_LINK: ${{ vars.PUBLIC_DISCORD_LINK }}
  PUBLIC_INSTAGRAM_LINK: ${{ vars.PUBLIC_INSTAGRAM_LINK }}
  RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
  RESEND_AUDIENCE_ID: ${{ secrets.RESEND_AUDIENCE_ID }}
  SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}

permissions:
  contents: write
  pull-requests: write
  pages: write
  id-token: write

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        if: github.event.action != 'closed'
        name: Install pnpm
        with:
          version: 10
          run_install: false

      - name: Set up Node.js
        if: github.event.action != 'closed'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Dependencies
        if: github.event.action != 'closed'
        run: pnpm install --frozen-lockfile

      - name: Build
        if: github.event.action != 'closed'
        run: VITE_BASE_URL="/pr-${{ github.event.number }}" pnpm build

      - name: Deploy PR preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./build
          umbrella-dir: /
          pages-base-url: preview.kalkul.app
